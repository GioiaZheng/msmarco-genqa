# Core scientific stack
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Configuration
pyyaml>=6.0

# Datasets and IO
datasets>=2.14
ir-datasets>=0.5.5            # official MS MARCO Passage corpus + dev/small

# Lexical retrieval
bm25s>=0.2                    # primary backend for the official Week 2 baseline

# Dense retrieval (Week 4)
sentence-transformers>=2.2
faiss-cpu>=1.7

# Generation
torch>=2.0
transformers>=4.35
sentencepiece>=0.1.99

# Evaluation
evaluate>=0.4
rouge_score>=0.1.2
sacrebleu>=2.3
bert_score>=0.3.13            # semantic-proxy generation eval (W6 sanity check)

# Optional serving/tracking extras are declared in pyproject.toml:
#   pip install -e ".[serve]"
#   pip install -e ".[tracking]"

# Testing
pytest>=7.0
